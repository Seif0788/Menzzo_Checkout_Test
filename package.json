{
  "name": "for_test",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "allure:clean": "rm -rf allure-results allure-report",
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed",
    "test:allure": "npx playwright test --reporter=line,allure-playwright",
    "allure:generate": "npx allure generate ./allure-results --clean -o ./allure-report",
    "allure:open": "npx allure open ./allure-report",
    "test:multi": "npx rimraf allure-results allure-report && npx playwright test && npx allure generate allure-results --clean -o ./allure-report",
    "test:multi:open": "npx rimraf allure-results allure-report && npx playwright test && npx allure generate allure-results --clean -o ./allure-report && npx allure open ./allure-report",
    "test:fr": "npx rimraf allure-results allure-report && npx playwright test --project=\"FR Checkoutsuite\" && npx allure generate allure-results --clean -o ./allure-report && npx allure open ./allure-report",
    "test:de": "npx rimraf allure-results allure-report && npx playwright test --project=\"DE Checkoutsuite\" && npx allure generate allure-results --clean -o ./allure-report && npx allure open ./allure-report",
    "test:it": "npx rimraf allure-results allure-report && npx playwright test --project=\"IT Checkoutsuite\" && npx allure generate allure-results --clean -o ./allure-report && npx allure open ./allure-report",
    "test:Homepage": "npx rimraf allure-results allure-report && npx playwright test --project=\"Homepage\" && npx allure generate allure-results --clean -o ./allure-report && npx allure open ./allure-report",
    "test:all-suites:open": "npx rimraf allure-results allure-report && npx playwright test && npx allure generate ./allure-results --clean -o ./allure-report && start npx allure open ./allure-report",
    "test:e2e": "npx playwright test",
    "test:price": "npx playwright test --project=Product_Price && npx allure generate allure-results --clean -o ./allure-report && npx allure open ./allure-report",
    "browserstack": "npx browserstack-node-sdk playwright test",
    "browserstack:mobile": "npx browserstack-node-sdk playwright test --project=\"Mobile_iPhone_13\"",
    "browserstack:seo": "npx browserstack-node-sdk playwright test tests/SEO"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "@types/langdetect": "^0.2.2",
    "@types/papaparse": "^5.5.0",
    "allure-commandline": "^2.34.1",
    "allure-playwright": "^3.4.1",
    "browserstack-node-sdk": "^1.38.1",
    "rimraf": "^6.0.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "franc": "^6.2.0",
    "langdetect": "^0.2.1",
    "papaparse": "^5.5.3"
  }
}
